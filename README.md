Udacity _Teaching an LLM to Count_ project: fine-tuning a Qwen2.5-3B-Instruct LLM with LoRA.

Needs a GPU with at least 15GB of VRAM. The notebook installs its dependencies (`unsloth`, `vllm`).

Then start JupyterLab with: `jupyter lab` and open `gen_ai_fundamentals_project_starter.ipynb`.
